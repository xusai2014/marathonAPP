import { Factory } from  'meteor/factory';
import React from 'react';
import {shallow } from 'enzyme';
import {chai} from 'meteor/practicalmeteor:chai';
import LoginTest from '../import/ui/login.jsx';

describe('LoginTest', () => {
  it('should render', () => {
    
  });
});